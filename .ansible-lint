exclude_paths:
  - .git/
  - collections/
skip_list:
  - yaml[line-length]
  # This repo still exposes many inventory-facing role variables without strict
  # role-name prefixes. Keep the sub-rule disabled until a dedicated rename
  # refactor is planned across the full role set.
  - var-naming[no-role-prefix]
