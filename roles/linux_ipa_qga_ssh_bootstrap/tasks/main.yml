---
- name: Run controller-side Linux QGA SSH bootstrap preparation
  ansible.builtin.include_tasks: controller.yml
  when: inventory_hostname == 'localhost'

- name: Run Proxmox-side Linux QGA SSH bootstrap execution
  ansible.builtin.include_tasks: proxmox.yml
  when: inventory_hostname != 'localhost'
